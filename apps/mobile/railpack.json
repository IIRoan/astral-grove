{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "bun": "1.4.2"
  },
  "steps": {
    "build": {
      "commands": [
        "bun run --filter @riftbound/contracts build",
        "bun run --cwd apps/mobile build:web"
      ]
    }
  }
}
